# Fenced Code Note

Some intro text.

```python
# this looks like a header but is inside a fence
def f():
    return 1
```

#standalone-tag is an inline tag with no space after the hash in `#nospacetag`.

## Real Heading After Fence

More text after the fence, under a real ATX heading.
