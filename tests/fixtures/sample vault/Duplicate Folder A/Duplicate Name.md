# Duplicate Name

One of two notes sharing the basename "Duplicate Name", in folder A (shorter path than folder B
when both are the same depth — used to test the "ambiguous basename resolves to shortest path,
ties broken lexicographically" rule together with its counterpart in `Duplicate Folder B/`).
